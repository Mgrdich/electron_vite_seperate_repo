{
  "name": "electron",
  "version": "1.0.0",
  "description": "",
  "main": "main.cjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron-forge start",
    "package": "electron-forge package",
    "make": "electron-forge make",
    "electron:pre_dev": "cross-env IS_DEV=true electron-forge start",
    "electron:build": "npm run web:build && electron-forge make",
    "electron:package": "electron-forge package",
    "electron:dev": "concurrently -k \"npm run web:dev\" \"npm run electron:pre_dev\"",
    "web:dev": "npm run dev --prefix ../",
    "web:build": "npm run build --prefix ../"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@electron-forge/cli": "^6.1.1",
    "@electron-forge/maker-deb": "^6.1.1",
    "@electron-forge/maker-rpm": "^6.1.1",
    "@electron-forge/maker-squirrel": "^6.1.1",
    "@electron-forge/maker-zip": "^6.1.1",
    "concurrently": "^8.0.1",
    "cross-env": "^7.0.3",
    "electron": "24.2.0"
  },
  "dependencies": {
    "electron-squirrel-startup": "^1.0.0"
  }
}
